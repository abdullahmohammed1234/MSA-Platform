/**
 * Dawah Academy is hidden until student-facing content is ready.
 * Set VITE_ACADEMY_ENABLED=true to re-enable academy routes and public auth.
 */
export const isAcademyEnabled = import.meta.env.VITE_ACADEMY_ENABLED === 'true';

export const isPublicAuthEnabled = true;
