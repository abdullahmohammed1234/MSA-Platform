export interface CheckboxProps {
  modelValue: boolean | any[];
  value?: any;
  label?: string;
  description?: string;
  error?: string;
  disabled?: boolean;
  required?: boolean;
  indeterminate?: boolean;
}
